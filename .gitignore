# Raw data snapshots (reproducible via geo_metadata_fetcher.py)
data/
geo_metadata.json

# Derived files (reproducible via pipeline scripts)
rnaseq_classified.json
chipseq_classified.json
methylation_classified.json
multiomics_classified.json
ftp_index.json
ftp_probe_results.json

# Combined search index (reconstructable by concatenating per-assay shards)
wiki/search_index.txt

# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Claude Code
.claude/
